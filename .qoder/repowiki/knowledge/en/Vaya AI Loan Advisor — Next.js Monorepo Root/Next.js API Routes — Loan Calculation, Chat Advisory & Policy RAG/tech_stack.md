Next.js App Router Route Handlers with `NextRequest`/`NextResponse`; Server-Sent Events via native `ReadableStream` + `TextEncoder`; Zod-based schema validation (`LoanProfileSchema`); LLM abstraction through `getLLMProvider()` (Qwen/DashScope primary, DeepSeek fallback per project intent); RAG implemented with in-memory embedding + cosine retrieval (`embedText`, `retrieveTopK`, `getAllChunks`).